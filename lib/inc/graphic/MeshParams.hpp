#pragma once

// TODO use File instead of const char*
struct MeshParams
{
    const char* object;
    const char* diffuseTexture;
    const char* metallicTexture;
    const char* roughTexture;
    const char* normalTexture;

};
